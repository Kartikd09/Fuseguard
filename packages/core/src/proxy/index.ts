// Proxy: request handling, transparent forward, SSRF allowlist, fail-open/closed switch.
// MIT/OSS (ARCHITECTURE §7).
//
// TODO(ROADMAP Phase 1, tasks 3-4, 12-13 / FR-1): FuseGuard key authn, transparent forward to
// api.anthropic.com (byte-identical response), SSRF-constant upstream, fail-closed/open policy,
// zod boundary validation + safe error mapper.

export {};
